spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tamtamcatworks
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB
auction:
  anti-snipe:
    window-seconds: 120
    extension-seconds: 120

minio:
  endpoint: http://localhost:9000
  externalUrl: http://localhost:9000
  accessKey: minioadmin
  secretKey: minioadmin
  bucketName: auction-images
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <artifactId>auction-system</artifactId>
    <groupId>org.tamtamcatworks.auction</groupId>
    <version>1.1.0</version>
  </parent>

  <groupId>org.tamtamcatworks.auction</groupId>
  <artifactId>client</artifactId>
  <packaging>jar</packaging>

  <name>client</name>

  <properties>
      <javafx.version>22</javafx.version>
      <app.mainClass>org.tamtamcatworks.auction.client.Launcher</app.mainClass>
      <app.moduleName>org.tamtamcatworks.auction.client</app.moduleName>
      <app.name>Auction System</app.name>
      <app.version>${project.version}</app.version>
      <lib.atlantafx.version>2.0.1</lib.atlantafx.version>
      <lib.ikonli.version>12.3.1</lib.ikonli.version>
      <lib.gemsfx.version>4.0.5</lib.gemsfx.version>
  </properties>

  <dependencies>
      <!-- JavaFX -->
      <dependency>
          <groupId>org.openjfx</groupId>
          <artifactId>javafx-controls</artifactId>
          <version>${javafx.version}</version>
      </dependency>
      <dependency>
          <groupId>org.openjfx</groupId>
          <artifactId>javafx-fxml</artifactId>
          <version>${javafx.version}</version>
      </dependency>

      <!-- AtlantaFX theme -->
      <dependency>
          <groupId>io.github.mkpaz</groupId>
          <artifactId>atlantafx-base</artifactId>
          <version>${lib.atlantafx.version}</version>
      </dependency>

      <!-- Ikonli icons -->
      <dependency>
          <groupId>org.kordamp.ikonli</groupId>
          <artifactId>ikonli-javafx</artifactId>
          <version>${lib.ikonli.version}</version>
      </dependency>
      <dependency>
          <groupId>org.kordamp.ikonli</groupId>
          <artifactId>ikonli-materialdesign2-pack</artifactId>
          <version>${lib.ikonli.version}</version>
      </dependency>

      <!-- GemsFX controls -->
      <dependency>
          <groupId>com.dlsc.gemsfx</groupId>
          <artifactId>gemsfx</artifactId>
          <version>${lib.gemsfx.version}</version>
      </dependency>

      <!-- HTTP client -->
      <dependency>
          <groupId>org.springframework</groupId>
          <artifactId>spring-web</artifactId>
          <version>6.1.6</version>
      </dependency>
      <dependency>
          <groupId>org.springframework</groupId>
          <artifactId>spring-websocket</artifactId>
          <version>6.1.6</version>
      </dependency>
      <dependency>
          <groupId>org.springframework</groupId>
          <artifactId>spring-messaging</artifactId>
          <version>6.1.6</version>
      </dependency>

      <!-- JSON -->
      <dependency>
          <groupId>com.fasterxml.jackson.core</groupId>
          <artifactId>jackson-databind</artifactId>
      </dependency>
      <dependency>
          <groupId>com.fasterxml.jackson.datatype</groupId>
          <artifactId>jackson-datatype-jsr310</artifactId>
      </dependency>

      <!-- shared DTOs -->
      <dependency>
          <groupId>org.tamtamcatworks.auction</groupId>
          <artifactId>shared</artifactId>
      </dependency>
  </dependencies>

  <build>
      <resources>
          <resource>
              <directory>src/main/resources</directory>
              <filtering>false</filtering>
              <excludes>
                  <exclude>application.properties</exclude>
              </excludes>
          </resource>
          <resource>
              <directory>src/main/resources</directory>
              <filtering>true</filtering>
              <includes>
                  <include>application.properties</include>
              </includes>
          </resource>
      </resources>

      <plugins>
          <plugin>
              <groupId>org.openjfx</groupId>
              <artifactId>javafx-maven-plugin</artifactId>
              <version>0.0.8</version>
              <configuration>
                  <executable>${java.home}/bin/java</executable>
                  <mainClass>${app.mainClass}</mainClass>
              </configuration>
              <executions>
                  <execution>
                      <id>run</id>
                  </execution>
                  <execution>
                      <id>debug</id>
                      <configuration>
                          <options>
                              <option>-agentlib:jdwp=transport=dt_socket,server=y,suspend=y,address=*:5005</option>
                          </options>
                      </configuration>
                  </execution>
              </executions>
          </plugin>
      </plugins>
  </build>

</project>